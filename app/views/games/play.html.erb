<h1>Let's Play</h1>
<h4>Q.1 </h4>
<% select = 0 %>
<table>
  <% @questions.each do |question| %>
    <tr>
      <% select += 1 %>
      <th><%= select %>:
      <%= question.name %></th>
      <th><input type="button" class="btn btn-primary" value="click!"></th>
    </tr>
    <% end %>
</table>

<span class="text-success">genre:<%= @genre.name %></span>